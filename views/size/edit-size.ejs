<%- include('../includes/head.ejs') %>
</head>

<body>
  <%- include('../includes/navigation.ejs') %>

  <main class="row vertical-center">
    <div class="col-xs-8 col-xs-offset-2  col-sm-6 col-sm-offset-3 col-md-4 col-sm-offset-4 col-lg-8 col-lg-offset-5s">
      <span for="name" class="fs-3" style="display: flex;
        margin-bottom: 20px;">Enter Size Type:</span>
      <form class="form-floating " action="/<% if (editing) { %>edit-size<% } else { %>add-size<% } %>" method="POST">
        <input type="text" class="form-control form-control-lg" name="name" id="floatingInput" value="<% if (editing) { %><%= size.type %><% } %>">
        <label for="floatingInput">for example:  1100R20</label>

        <% if (editing) { %>
        <input type="hidden" value="<%= size.id %>" name="sizeId">
        <% } %>
        <div>
          <button type="submit" class="btn btn-lg btn-outline-success" style="margin-top: 20px;">
            <% if (editing) { %>Update Size<% } else { %>Add Size<% } %>
          </button>
        </div>
      </form>
    </div>
  </main>
  <%- include('../includes/end.ejs') %>